# apis/proto

Optional **non-Rust** home for `.proto` files used by `grpcio-tools` / `buf`. **Canonical protos** for the running cluster remain under **`engine/`** until you split repos; see [docs/grpc-codegen.md](../docs/grpc-codegen.md).
