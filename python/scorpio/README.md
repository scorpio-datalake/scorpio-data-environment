# scorpio (Python client)

Editable stub (`pip install -e python/scorpio`). Runtime deps in `pyproject.toml`; gRPC stubs → `src/scorpio/_grpc/` per [docs/grpc-codegen.md](../docs/grpc-codegen.md). Epic 1 in `Planning/Phase1.md`.
