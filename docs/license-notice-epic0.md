# Epic 0 — LICENSE / NOTICE

No engine Rust sources were modified in the Epic 0 deploy/Python/k8s closure. Third-party additions: Python wheels from PyPI in `python/*/pyproject.toml` (Apache-compatible licenses); container base images (`python`, `nginx`, `hashicorp/http-echo`) per vendor terms.

Action: before each release, re-read `engine/LICENSE`, `engine/NOTICE`, and upstream notices in vendored/generated code.
