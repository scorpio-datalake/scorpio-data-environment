# Copy to `.env` next to this file (optional) or export in your shell before `docker compose`.
# Must be an absolute path to the directory that contains the engine workspace `Cargo.toml`.

SCORPIO_ENGINE_ROOT=/absolute/path/to/scorpio-data-environment/engine
